# ISPyB Logistics (Dewar Management)

## Setup
Create a virtualenv then install with pip

pip install -r requirements.txt

## Configuration
export ISPYB_CONFIG_FILE=<path to credentials file>

Format is as specified by ispyb-python-api

Example in tests/test.cfg

## Running
$ python app.py

## Security
Currently a prototype so authentication not provided!

